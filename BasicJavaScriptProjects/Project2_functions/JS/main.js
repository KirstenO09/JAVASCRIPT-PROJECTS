function concatenate(){
    var sentence = "I am learning";// This is the first part of the sentence
    sentence += " a lot from this course!";// This is the second part of the sentence
    document.getElementById("Concatenate").innerText = sentence; // This concatenates the two parts of the sentence and displays it in an HTML element with the ID "Concatenate".
} // This function concatenates two strings and displays the result in an HTML element with the ID "Concatenate".